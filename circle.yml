test:
    override:
        - echo hello world!
        - aws --version
        - cat vpc.yml
        - pwd=`pwd`
        - aws cloudformation validate-template --template-body file:////${pwd}/vpc.yml