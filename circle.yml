test:
    override:
        - aws configure set default.region ap-northeast-1
        - aws cloudformation validate-template --template-body file:////`pwd`/vpc.yml

deployment:
  prod:
    branch: master
    commands:
      - aws cloudformation update-stack --stack-name myteststacks --template-body file:////`pwd`/vpc.yml